# Зависимости для разработки и тестов
# Устанавливать: pip install -r requirements-dev.txt
# Или (рекомендуется): pip install -e ".[dev]"

# ─── Тестирование ───
pytest>=7.0
pytest-cov>=4.0
pytest-benchmark>=4.0
pytest-asyncio>=0.23.0
hypothesis>=6.0
lxml>=4.9.0
Pillow>=10.0.0

# ─── Линтеры и тайп-чекинг ───
ruff>=0.5.0
mypy>=1.10

# ─── Структурированное логирование ───
structlog>=24.0

# ─── Pre-commit hooks ───
pre-commit>=3.7

# ─── MCP SDK (для запуска mcp_server.py) ───
mcp>=1.0.0

# ─── Граф зависимостей (для dependency_graph.py) ───
networkx>=3.0

# ─── Сборка EPF (для epf_factory.py) ───
v8unpack>=1.2.6
