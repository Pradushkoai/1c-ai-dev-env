# Опциональные — для RAG с нейросетевыми embeddings
# TF-IDF поиск (fast_search_1c.py) работает БЕЗ этих пакетов
fastembed>=0.8.0
qdrant-client>=1.0.0
# MCP-сервер — для интеграции с IDE/LLM (Cursor, Claude Desktop, VS Code)
# Команда: 1c-ai mcp serve
mcp>=1.0.0
