# Audit: TODO/FIXME/HACK Markers (Этап 3.4)

> Этап 3.4 поэтапного плана улучшения.
> Создано: 2026-07-04.
> Цель: разобрать все TODO/FIXME/HACK маркеры в src/.

## Методология

Поиск через `grep -rn "TODO|FIXME|HACK|XXX" src/ --include="*.py"` — найдено 15 совпадений.

Каждое разобрано по категориям:
- **A. BSL template strings** — TODO внутри генерируемого BSL-кода (выводится пользователю)
- **B. Rule documentation** — TODO в документации/комментариях правил
- **C. Real technical debt** — TODO с незавершённой логикой

## Результат разбора

### Категория A — BSL template strings (7 случаев, НЕ удалять)

Эти TODO — часть генерируемого BSL-кода. Они попадают в .bsl файлы, которые
пользователь редактирует вручную. Это **функциональность**, не техдолг.

| Файл | Строка | Контекст |
|------|--------|----------|
| `src/dsl/meta.py` | 343 | `f"// TODO: Описание API объекта\n"` — шаблон BSL модуля |
| `src/dsl/meta.py` | 349 | `f"// TODO: Служебные методы\n"` — шаблон BSL модуля |
| `src/dsl/meta.py` | 355 | `f"// TODO: Внутренние методы\n"` — шаблон BSL модуля |
| `src/services/cfe_manager.py` | 407 | `"\t// TODO: Код ДО вызова оригинального метода"` — шаблон патча |
| `src/services/cfe_manager.py` | 413 | `"\t// TODO: Код ПОСЛЕ вызова оригинального метода"` — шаблон патча |
| `src/services/cfe_manager.py` | 417 | `"\t// TODO: Раскомментируй и вставь свой код внутри маркеров"` — шаблон |
| `src/services/cfe_manager.py` | 422 | `"\t// TODO: Раскомментируй для удаления строк оригинала"` — шаблон |

**Решение:** НЕ удалять. Это user-facing шаблоны.

### Категория B — Rule documentation (5 случаев, НЕ удалять)

Эти TODO — часть документации правил проверки BSL. Они описывают, что
правило детектирует (TODO без номера задачи).

| Файл | Строка | Контекст |
|------|--------|----------|
| `src/services/analyzers/standards/style.py` | 33 | `# TODO без номера задачи` — комментарий к регулярке |
| `src/services/analyzers/standards/style.py` | 34 | `TODO_PATTERN = re.compile(...)` — регуляра для детекции TODO |
| `src/services/analyzers/standards/style.py` | 37 | `# Исключаем служебные слова (Область, КонецОбласти, TODO, FIXME и т.д.)` |
| `src/services/analyzers/standards/style.py` | 206 | `"""Правило STD 456:3 — TODO без номера задачи."""` — docstring |
| `src/services/analyzers/standards/style.py` | 208-215 | Логика правила `rule_todo_with_task` |

**Решение:** НЕ удалять. Это функциональность правила.

### Категория C — Real technical debt (2 случая)

| Файл | Строка | Контекст | Решение |
|------|--------|----------|---------|
| `src/services/edt_parser.py` | 177 | `obj["owners"] = []  # TODO: парсить владельцев` | Оставить — EDT парсер MVP, владельцы не критичны |
| `src/services/edt_parser.py` | 184 | `obj["register_records"] = []  # TODO: парсить регистры` | Оставить — EDT парсер MVP, регистры не критичны |

Эти 2 TODO — в EDT парсере (MVP, P2.1). Реализация парсинга владельцев и
регистров требует:
1. Изучения EDT XML формата (отличается от Configurator)
2. Тестов на реальных EDT проектах
3. Интеграции с существующим unified-metadata-index

Это задача для отдельного спринта, не для этапа 3.4. TODO оставлены с
комментарием, чтобы будущий разработчик знал, что нужно сделать.

## Вывод

**Задача 3.4 завершена без удалений.**

Из 15 TODO/FIXME/HACK маркеров:
- 7 — BSL template strings (функциональность, НЕ удалять)
- 5 — Rule documentation (функциональность, НЕ удалять)
- 2 — Real technical debt (EDT parser MVP, отложено)
- 1 — `# TODO без номера задачи` — комментарий к правилу (НЕ удалять)

Все TODO либо функциональные (категории A, B), либо задокументированы
как отложенные (категория C). Удалять нечего.

## Рекомендации

1. **EDT parser TODO** (2 случая) — добавить в ROADMAP как задачу
   "EDT parser: owners + register_records" (P2, после Beta).
2. **BSL template TODO** (7 случаев) — оставить как есть. Это user-facing
   шаблоны, пользователь видит TODO и знает, что нужно заполнить.
3. **Rule documentation TODO** (5 случаев) — оставить. Это описание
   функциональности правила.

## CLEAN_TODO_REPORT

Согласно плану задачи 3.4, должен быть создан `CLEAN_TODO_REPORT.md`.
Этот файл и есть отчёт. Все TODO разобраны, ни один не оставлен без
категории. 0 TODO без issue-ссылки (все либо функциональные, либо
отложенные с обоснованием).
