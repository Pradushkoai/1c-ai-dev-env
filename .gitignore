# Данные пользователя
data/
# Производные (генерируются)
derived/
# Инструменты (клонируются в корень проекта — top-level only)
/tools/
# Runtime (состояние пользователя — config-registry, task-log, и т.д.)
# Не коммитить: это user-specific состояние, генерируется при работе.
runtime/
# Learning loop
learned-skills/
# Временные
*.tmp
*.bak
__pycache__/
**/__pycache__/
*.pyc
*.pyo
*.egg-info/
*.egg
build/
dist/
# Системные
.env
upload/
# Бинарные артефакты 1С — публикуются через GitHub Releases, не в VCS
download/
skills/
runtime/bsl-baseline.json

# Sphinx build artifacts (генерируются через sphinx-build, не в VCS)
docs/sphinx/_build/

# Coverage
.coverage
coverage.xml
coverage_html/
.sarif-reports/
sarif-reports/

# Ruff / Mypy кэш
.ruff_cache/
.mypy_cache/

# pytest-benchmark
.benchmarks/

# Credentials
*.credentials
.git-credentials

# IDE и редакторы (P3.20)
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Логи (P3.20)
*.log
logs/

# pytest кэш
.pytest_cache/

# venv
.venv/
venv/
env/


# P2.7: git bundle backups (не коммитить)
*.bundle
