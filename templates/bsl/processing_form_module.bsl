// Шаблон: Модуль формы обработки (управляемая форма)
// Заменяемые переменные: {{NAME}}, {{SYNONYM}}

#Область ОписаниеПеременных

// TODO: Добавьте переменные формы

#КонецОбласти

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Инициализация формы обработки {{NAME}}
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ВыполнитьОбработку(Команда)
	
	// Вызов серверной процедуры выполнения
	ВыполнитьОбработкуНаСервере();
	
КонецПроцедуры

&НаСервере
Процедура ВыполнитьОбработкуНаСервере()
	
	// Вызов метода модуля объекта
	// Объект — это реквизит формы типа "СправочникСсылка.{{NAME}}" или "Обработка"
	РеквизитФормыВЗначение("Объект").ВыполнитьОбработку();
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// TODO: Добавьте служебные процедуры и функции

#КонецОбласти
